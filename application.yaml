apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: myapp-argo-application
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/Jocasmen94/argocd-test.git
    targetRevision: HEAD
    path: kubernetes-manifests
  destination: 
    server: https://kubernetes.default.svc
    namespace: acme-fitness

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true
      